const config = {}

config.githubToken = 'your github token is needed'
config.githubUsername = 'your github username'
config.githubClient = 'yout github client id'
config.githubSecret = 'your github client secret'

module.exports = config
