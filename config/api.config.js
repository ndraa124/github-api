const config = {}

config.githubToken = 'your token github'
config.githubUsername = 'ndraa124'
config.githubClient = 'your client id'
config.githubSecret = 'your client secret'

module.exports = config
